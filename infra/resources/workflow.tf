# Workflowsのリソース定義

# resource "google_workflows_workflow" "baseball_management" { # resource type 固定、ローカル識別子 任意
#   name            = "baseball-management"            # 必須：実ワークフロー名と一致
#   region          = var.region                       # 必須：実リージョンと一致
#   # source_contents = <<-EOF                           # 任意：YAML/JSON のソース内容
# }
